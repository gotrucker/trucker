exit_on_error: false
connections:
- name: pgconn
  adapter: postgres
  host: {{ or .PG_HOST "localhost" }}
  database: postgres
  user: postgres
  pass: pgpass
  replica_host: a.b.c.d
  replica_host_path: ../replica_host_secret
